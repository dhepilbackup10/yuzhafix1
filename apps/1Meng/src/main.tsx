import React from 'react'
import ReactDOM from 'react-dom/client'
import MengApp from './MengApp'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <MengApp />
  </React.StrictMode>,
)